name: Deploy to Render

on:
  push:
    branches:
      - master  # or your deploy branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy (Frontend)
        run: curl "https://api.render.com/deploy/srv-d1gof8idbo4c73b256i0?key=EazVkibpxxM"

      - name: Trigger Render Deploy (Backend)
        run: curl "https://api.render.com/deploy/srv-d1go0onfte5s738thm6g?key=yTDsLdanmKI" 

